import React from "react";
import Home from "./homepage/home";  // Importamos la página principal

const Page = () => {
  return <Home />;  // Renderizamos la página de inicio
};

export default Page;
